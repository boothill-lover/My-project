using UnityEngine;

public class MyFirstSpecialScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("Hello World!");
        print("Welcome to Unity brah");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
